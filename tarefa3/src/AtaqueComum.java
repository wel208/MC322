public class AtaqueComum implements AcaoDeCombate{
    
    public void executar(Combatente usuario, Combatente alvo){
        if (usuario.getArma().getTipo() == "Longo Alcance"){

        }
        else{

        }
    }
}
